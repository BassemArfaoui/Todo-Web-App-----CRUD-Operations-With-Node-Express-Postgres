<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Travel Tracker</title>

  <!-- google fonts -->
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0" />
  <!-- bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha2/dist/css/bootstrap.min.css" rel="stylesheet"
  integrity="sha384-aFq/bzH65dt+w6FI2ooMVUpc+21e0SRygnTpmBvdBgSdnuTN7QbdgL+OapgHtvPp" crossorigin="anonymous">
  <!-- css file -->
  <link rel="stylesheet" href="./styles/main.css">

  <style>

  </style>
</head>

<body>

    <div  class="container-fluid w-100 bg-dark d-flex justify-content-center align-items-center flex-column gap-4" style="height:100vh">
        <p class="text-danger m-0  p-0 fw-bold"><%= locals.error ? error : '' %></p>
        <% if(locals.todo) { %>
        <form  action="/edit/<%= todo.id %>" method="POST" class="input w-50 d-flex gap-3">
          <input type="text" class="form-control fw-bold w-75 border-3 border-info todo_input" id=""  style="height:45px" name="todo_to_edit" autocomplete="off"  value="<%= locals.todo ? todo.title : '' %> ">
          <input type="submit" value="EDIT" class="btn bg-primary w-25 fw-bold" name="update">
        </form>
        <% } else {%>
         <a href="/" class="btn bg-primary fw-bold">Home</a>
        <% } %>
      </div>

</body>

</html>